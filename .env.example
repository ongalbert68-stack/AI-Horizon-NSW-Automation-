# Used by `docker compose` / `podman compose` to configure the local Postgres
# container. Copy to `.env` in this same directory. Keep these in sync with
# backend/.env (POSTGRES_* there must match what you set here).
POSTGRES_USER=ai_horizon
POSTGRES_PASSWORD=ai_horizon
POSTGRES_DB=ai_horizon_nsw
POSTGRES_PORT=5433
